import 'package:flutter/material.dart';

/// Palette principale Collecte Pro — thème vert Express Exchange.
class AppColors {
  AppColors._();

  /// Couleur principale (boutons, AppBar, accents).
  static const Color primary = Color(0xFF1B8A3A);

  /// Variante plus foncée pour contrastes (bordures, icônes actives).
  static const Color primaryDark = Color(0xFF146B2E);

  /// Fond secondaire / surfaces légères.
  static const Color surfaceMuted = Color(0xFFF5F5F5);

  /// Texte principal sur fond clair.
  static const Color textPrimary = Color(0xFF1A1A1A);

  /// Texte secondaire.
  static const Color textSecondary = Color(0xFF6B7280);

  /// Erreur / alerte.
  static const Color error = Color(0xFFB91C1C);

  /// Succès.
  static const Color success = Color(0xFF15803D);

  /// Fond de l’application (écrans).
  static const Color background = Colors.white;
}
